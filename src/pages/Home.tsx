import Button from '@components/Button';

const Home = (): JSX.Element => {
  return (
    <div>
      <p>Home</p>
      <div>
        <Button />
      </div>
    </div>
  );
};

export default Home;
