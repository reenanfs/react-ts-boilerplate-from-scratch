import Button from '@components/Button';

const Home = (): JSX.Element => {
  return (
    <div className="home">
      <p>Home</p>
      <div>
        <Button />
      </div>
    </div>
  );
};

export default Home;
